import {NextResponse} from "next/server";import {db} from "@/lib/db";import {ensureDatabase} from "@/lib/setup";export const dynamic="force-dynamic";export async function GET(){try{await ensureDatabase();const r=await db()`SELECT state,version,updated_at FROM county_governance_state WHERE id=1`;return NextResponse.json({state:r[0]?.state||{agencies:[],government:{}},version:r[0]?.version||0,updatedAt:r[0]?.updated_at||null})}catch(e){console.error(e);return NextResponse.json({error:"County Governance unavailable"},{status:500})}}