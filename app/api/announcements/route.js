import {NextResponse} from "next/server";import {db} from "@/lib/db";import {ensureDatabase} from "@/lib/setup";export const dynamic="force-dynamic";export async function GET(){try{await ensureDatabase();const rows=await db()`SELECT id,title,summary,body,category,published_at FROM county_public_announcements WHERE active=TRUE AND (expires_at IS NULL OR expires_at>NOW()) ORDER BY published_at DESC LIMIT 50`;return NextResponse.json({announcements:rows})}catch(e){console.error(e);return NextResponse.json({error:"Announcements unavailable"},{status:500})}}