import {NextResponse} from "next/server";import {db} from "@/lib/db";import {ensureDatabase} from "@/lib/setup";export const dynamic="force-dynamic";
function normalize(v=''){return String(v).trim().toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/&/g,' and ').replace(/[^a-z0-9]+/g,' ').trim().replace(/\s+/g,' ')}
export async function GET(){try{await ensureDatabase();const rows=await db()`SELECT code,name,rate_basis_points,description FROM county_tax_rules WHERE active=TRUE ORDER BY sort_order,name`;return NextResponse.json({rules:rows})}catch(e){console.error(e);return NextResponse.json({error:'Tax rules unavailable'},{status:500})}}
export async function POST(req){try{await ensureDatabase();const b=await req.json();const businessName=String(b.businessName||'').trim(),category=String(b.categoryCode||''),direction=b.direction==='expense'?'expense':'income',amount=Number(b.amount),reporter=String(b.reporterName||'').trim();if(!businessName||!category||!reporter||!Number.isFinite(amount)||amount<=0)return NextResponse.json({error:'Business, reporter, category and positive amount required.'},{status:400});const sql=db();const rr=await sql`SELECT code,name,rate_basis_points FROM county_tax_rules WHERE code=${category} AND active=TRUE LIMIT 1`;if(!rr.length)return NextResponse.json({error:'Unknown tax category.'},{status:400});const rule=rr[0],signed=direction==='expense'?-amount:amount,tax=Math.round(signed*Number(rule.rate_basis_points)/10000*100)/100,n=normalize(businessName);const alias=(await sql`SELECT economic_profile_id FROM county_business_aliases WHERE normalized_name=${n} LIMIT 1`)[0];const rows=await sql`INSERT INTO county_business_tax_ledger(source,record_kind,business_name,normalized_business_name,player_name,category_code,tax_rate_basis_points,gross_amount,tax_amount,occurred_at,economic_profile_id,metadata) VALUES('county_public_filing','filing',${businessName},${n},${reporter},${category},${Number(rule.rate_basis_points)},${signed},${tax},${b.occurredAt||new Date().toISOString()},${alias?.economic_profile_id||null},${JSON.stringify({description:b.description||'',certified:Boolean(b.certified),direction,contact:b.contact||''})}::jsonb) RETURNING public_id,tax_amount,economic_profile_id`;return NextResponse.json({ok:true,reference:rows[0].public_id,taxImpact:Number(rows[0].tax_amount),linkedProfile:rows[0].economic_profile_id||null})}catch(e){console.error(e);return NextResponse.json({error:'Filing could not be stored.'},{status:500})}}
